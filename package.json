{
  "name": "emiratesnbd-web-framework",
  "version": "1.0.0",
  "description": "Emirates NBD web journey + link integrity framework — Playwright + TypeScript, self-healing locators, CI/CD to AWS",
  "scripts": {
    "test": "playwright test",
    "test:journey": "playwright test --grep @journey",
    "test:links": "playwright test tests/home-link-integrity.spec.ts",
    "test:headed": "playwright test --headed --project=chromium",
    "typecheck": "tsc --noEmit",
    "report": "playwright show-report",
    "baseline:update": "UPDATE_BASELINE=1 playwright test tests/home-link-integrity.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^26.4.1",
    "typescript": "^7.0.2"
  }
}
